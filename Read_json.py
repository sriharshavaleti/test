import json
import os
import sys

json_data = sys.argv[1]
filename=os.path.basename(localpath)
with open(filename, 'r') as f:
    datastore = json.load(f)
    print(datastore)
for i in datastore
     for k in json_data
          if datastore[i] =! json_data[j]
               print(datastore[i])
